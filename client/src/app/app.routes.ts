import { Routes } from '@angular/router';
import { GuessPlayerName } from './pages/guess-player-name/guess-player-name';

export const routes: Routes = [{ path: 'guess-player-name', component: GuessPlayerName }];
